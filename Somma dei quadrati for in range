# Sum of Squares
number = int(input('Give us a number: '))
total = 0
for i in range(1, number + 1):  # Aggiungi 1 per includere anche il numero stesso
    square = i ** 2
    total += square  # Somma il quadrato di ogni numero al totale
print(total)
